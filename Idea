ML
我们现在的方法是，对每个标定点，一起算mu和sigma，然后做出来一个mu和sigma的表，相当于找特征值查表
能不能根据每个标定点很多的数据做出来一个更精细的查找表呢？——————————二维高斯
我可以把每个标定点的数据，做出来一个二维高斯曲线，这包含几个参数？查表过程相当于山峰的匹配。
特征参数都有什么？————山峰的峰位（x、y）、山峰的高度、山峰的二维展宽（一个椭圆？）但是这个椭圆与峰位之间是不是有联系？
————————————然后这个事情和DOI还有关系啊，这个事情我最开始忽略了，那可能平面分辨率只与峰位有关了啊，那就还是两个参数，但是还是可以用二维高斯模型来处理
剩下的参数来处理DOI
然后插值的事情，有没有必要做？意义？效果？
老版ML是对每个像素做高斯
每个标定点都生成一张表
然后新事例像素表*标定点mu sigma表，每个像素高斯函数相乘，然后对各个标定点都这么算，然后找一个最大的
就这样
那和我刚提出来的新思路有什么不一样的吗？
这么做我咋感觉精度更高呢
但是我觉得我新提出来的思路可能不属于极大似然了
